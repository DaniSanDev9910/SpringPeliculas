export type GeneroDto = {
    id: string,
    nombre: string
  };

export type Movie = {
    id: string,
    nombre: string,
    sinopsis: string,
    imagen: string,
    idGenero: string,
    director: string
  };
  